<div id="<%= dom_id post %>">
  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>

  <p>
    <strong>Body:</strong>
    <%= post.body %>
    <%= time_tag post.updated_at, "data-local": "time", "data-format": "%B %e, %Y %l:%m%p" %>
  </p>

</div>
